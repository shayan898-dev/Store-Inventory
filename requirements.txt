# Inventory Management System — Python Dependencies
# Install with: pip install -r requirements.txt

customtkinter>=5.2.0    # Modern dark-themed tkinter widgets
Pillow>=10.0.0          # Image support (optional: for product images)
pyinstaller>=6.0.0      # For compiling to .exe (run separately)
